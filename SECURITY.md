# 安全策略与漏洞披露规范 (Security Policy)

足迹 (Footprint) 非常重视系统安全性与用户个人数据隐私。如果您在本项目中发现了任何潜在的安全漏洞，请遵循以下流程与我们取得联系。

---

## 🛡️ 支持的安全版本

我们优先为最新的主干分支提供安全修复支持：

| 版本 / 分支 | 是否支持安全更新 |
| :--- | :--- |
| `main` 分支最新代码 | :white_check_mark: 支持 |
| 历史 Release 标签 | :warning: 建议更新至最新版 |

---

## 🔒 报告漏洞 (Reporting a Vulnerability)

**请勿通过公开的 GitHub Issue 发布安全漏洞细节。**

如果您发现了高危漏洞（如鉴权绕过、未授权数据访问、命令注入、XSS 漏洞等）：

1. 请通过 GitHub 仓库的 **Security Advisory**（私密漏洞报告功能）提交漏洞详情；
2. 或者直接联系项目维护者电子邮箱；
3. 在漏洞报告中，请尽可能包含：
   - 漏洞影响的组件与端点；
   - 详细的重现步骤（PoC 代码、请求抓包或截图）；
   - 漏洞可能产生的影响评估；
   - 如有修复建议，欢迎一并附上。

我们将在收到报告后 48 小时内完成初步评估，并在确认修复后发布更新公告与致谢。

---

## 💡 自托管运维安全最佳实践 (Self-Hosting Best Practices)

若您在公网服务器或 NAS 上部署 Footprint，建议参考以下安全配置：

1. **强随机 JWT 密钥**：
   - 不要使用弱口令作为 `JWT_SECRET_KEY`。在 Docker Compose 部署时挂载 `./data` 持久卷，系统将自动生成 32 字节强随机密钥并持久保存，避免重启容器后密钥变更导致用户登录失效。
2. **严格限制 CORS 跨域**：
   - 生产环境中请在 `.env` 中设置明确的 `CORS_ORIGINS`（例如 `https://your-domain.com`），避免使用通配符 `*` 允许未经授权的站点发起跨域请求。
3. **关闭 Flask 调试模式**：
   - 确保生产环境 `FLASK_ENV=production`，严禁在公网开放带有交互式调试器的开发模式。
4. **HTTPS 加密传输**：
   - 建议通过 Nginx、Caddy 或 Traefik 配置 SSL/TLS 证书反向代理，保障网络传输层不被中间人窃听或篡改。
